This is a testing version of simple RSS Reader written in Ruby on Rails.

Users can create accounts, add subscriptions, add filters for a subscription.

If a Reddit subscription is used and it has images in it, 
images will be shown directly on page (no need to open in new tab/window).

